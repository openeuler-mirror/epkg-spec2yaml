name: lvm2
meta.summary: Tools for logical volume management
version: 2.03.16
release: 1
epoch: 8
meta.license: GPLv2+ and LGPLv2.1 and BSD
meta.homepage: http://sourceware.org/lvm2
sources:
    - https://sourceware.org/pub/lvm2/releases/LVM2.2.03.16.tgz
patchset:
    - 0001-lvm2-set-default-preferred_names.patch
    - 0002-lvm2-default-allow-changes-with-duplicate-pvs.patch
    - 0003-bugfix-lvm2-add-SSD.patch
    - 0004-bugfix-add-timeout-when-fail-to-wait-udev.patch
    - 0005-bugfix-fix-the-code-maybe-lead-to-buffer-over-bound-access.patch
    - 0006-enhancement-modify-default-log-level-to-error-level.patch
    - 0007-enhancement-add-dfx-log.patch
    - 0008-enhancement-syslog-more-when-use-libdevmapper-so.patch
    - 0009-enhancement-log-it-when-disk-slow.patch
    - 0010-bugfix-lvm2-fix-the-reuse-of-va_list.patch
    - 0011-13-dm-disk.rules-check-DM_NAME-before-create-symlink.patch
    - 0012-lvm-code-reduce-cyclomatic-complexity.patch
    - 0013-_vg_read_raw_area-fix-segfault-caused-by-using-null-.patch
meta.description: lvm2 refers to the userspace toolset that provide logical volume management facilities on linux.
requires:
    - "bash >= %{bash_version}"
    - "module-init-tools"
    - "device-mapper-persistent-data >= %{persistent_data_version}"
    - "device-mapper-event = %{?epoch}:%{device_mapper_version}-%{release}"
requiresPreUn:
    - "systemd-units >= %{systemd_version}"
requiresPost:
    - "systemd-units >= %{systemd_version}, systemd-sysv"
requiresPostUn:
    - "systemd-units >= %{systemd_version}"
buildRequires:
    - "gcc"
    - "gcc-c++"
    - "libselinux-devel >= %{libselinux_version}, libsepol-devel"
    - "libblkid-devel >= %{util_linux_version}"
    - "ncurses-devel"
    - "readline-devel"
    - "libaio-devel"
    - "module-init-tools"
    - "pkgconfig"
    - "systemd-devel"
    - "systemd-units"
    - "python3-devel"
    - "python3-setuptools"
    - "python3-dbus"
    - "python3-pyudev"
    - "device-mapper-persistent-data >= %{persistent_data_version}"
    - "device-mapper-event"
    - "corosynclib-devel >= %{corosync_version} %if %{use_cluster}"
    - "dlm-devel >= %{dlm_version} %if %{use_cluster} || %{use_lockd_dlm}"
    - "sanlock-devel >= %{sanlock_version} %if %{use_lockd_sanlock}"
provides:
    - "%{name}-libs"
obsoletes:
    - "%{name}-libs"
conflicts:
    - "dracut < %{dracut_version}"
files: |
    
    %defattr(555,root,root,-)
    %{_sbindir}/fsadm
    %{_sbindir}/lvm
    %{_sbindir}/lvmconfig
    %{_sbindir}/lvmdump
    %{_sbindir}/lvmpolld
    %{_libdir}/liblvm2cmd.so.*
    %{_libdir}/libdevmapper-event-lvm2.so.*
    
    %defattr(444,root,root,-)
    %{_sbindir}/lvchange
    %{_sbindir}/lvconvert
    %{_sbindir}/lvcreate
    %{_sbindir}/lvdisplay
    %{_sbindir}/lvextend
    %{_sbindir}/lvmdiskscan
    %{_sbindir}/lvmsadc
    %{_sbindir}/lvmsar
    %{_sbindir}/lvreduce
    %{_sbindir}/lvremove
    %{_sbindir}/lvrename
    %{_sbindir}/lvresize
    %{_sbindir}/lvs
    %{_sbindir}/lvscan
    %{_sbindir}/pv*
    %{_sbindir}/vg*
    %{_sbindir}/lvmdevices
    %{_sbindir}/lvm_import_vdo
    
    %{_prefix}/lib/udev/rules.d/69-dm-lvm.rules
    %{_prefix}/lib/udev/rules.d/11-dm-lvm.rules
    
    %dir %{_sysconfdir}/lvm
    %ghost %{_sysconfdir}/lvm/cache/.cache
    %attr(644, -, -) %config(noreplace) %verify(not md5 mtime size) %{_sysconfdir}/lvm/lvm.conf
    %attr(644, -, -) %config(noreplace) %verify(not md5 mtime size) %{_sysconfdir}/lvm/lvmlocal.conf
    %dir %{_sysconfdir}/lvm/profile
    %{_sysconfdir}/lvm/profile/*
    %dir %{_sysconfdir}/lvm/backup
    %dir %{_sysconfdir}/lvm/cache
    %dir %{_sysconfdir}/lvm/archive
    %ghost %dir /run/lock/lvm
    %ghost %dir /run/lvm
    %dir %{_libdir}/device-mapper
    %{_libdir}/device-mapper/*
    %{_libdir}/libdevmapper-event-lvm2mirror.so
    %{_libdir}/libdevmapper-event-lvm2snapshot.so
    %{_libdir}/libdevmapper-event-lvm2raid.so
    %{_libdir}/libdevmapper-event-lvm2thin.so
    %{_libdir}/libdevmapper-event-lvm2vdo.so
    %{_tmpfilesdir}/%{name}.conf
    %{_unitdir}/blk-availability.service
    %{_unitdir}/lvm2-monitor.service
    %{_unitdir}/lvm2-lvmpolld.socket
    %{_unitdir}/lvm2-lvmpolld.service
    
recommends:
    - %{name}-help = %{epoch}:%{version}-%{release}
package:
    - Version:   %{device_mapper_version}
    - License:   LGPLv2
    - URL:       http://sources.redhat.com/dm
    - Requires:  util-linux >= %{util_linux_version}
    - Requires:  systemd >= %{systemd_version}
    - Provides:  device-mapper-libs = %{?epoch}:%{device_mapper_version}-%{release}
    - Obsoletes: device-mapper-libs
rpmMacros:
    - "%global use_cluster 1"
    - "%global use_cmirror 1"
    - "%global use_lockd_sanlock 1"
    - "%global use_lockd_dlm 1"
    - "%global use_cluster 0 %ifnarch i686"
    - "%global use_cmirror 0 %ifnarch i686"
    - "%global use_lockd_dlm 0 %ifnarch i686"
    - "%global use_lockd_sanlock 0 %ifnarch x86_64 aarch64"
    - "%global configure_cluster --with-cluster=internal --with-clvmd=corosync %if %{use_cluster}"
    - "%global configure_cmirror --enable-cmirrord %if %{use_cluster} %if %{use_cmirror}"
    - "%global configure_cluster --with-cluster=internal --with-clvmd=none %else %if %{use_cluster}"
    - "%global configure_lockd_dlm --enable-lvmlockd-dlm %if %{use_lockd_dlm}"
    - "%global configure_lockd_sanlock --enable-lvmlockd-sanlock %if %{use_lockd_sanlock}"
    - "%global libselinux_version 1.30.19-4"
    - "%global persistent_data_version 0.7.0-0.1.rc6"
    - "%global sanlock_version 3.3.0-2"
    - "%global device_mapper_version 1.02.182"
    - "%global systemd_version 189-3"
    - "%global dracut_version 002-18"
    - "%global util_linux_version 2.24"
    - "%global bash_version 4.0"
    - "%global corosync_version 1.99.9-1"
    - "%global resource_agents_version 3.9.5-12"
    - "%global dlm_version 4.0.6-2"
    - "%{?python_provide:%python_provide python3-lvm}"

subpackage:
    - name: devel
      meta.summary: lvm2 devel files
      meta.license: LGPLv2
      meta.description: This package contains all include files, libraries files needed to develop programs that use lvm2.
      requires:
          - "%{name} = %{?epoch}:%{version}-%{release}"
          - "pkgconfig"
          - "device-mapper = %{?epoch}:%{device_mapper_version}-%{release}"
      provides:
          - "device-mapper-devel = %{?epoch}:%{device_mapper_version}-%{release}"
          - "device-mapper-event-devel = %{?epoch}:%{device_mapper_version}-%{release}"
      obsoletes:
          - "device-mapper-devel"
          - "device-mapper-event-devel"
      files: |
          %defattr(444,root,root,-)
          %{_libdir}/liblvm2cmd.so
          %{_libdir}/libdevmapper-event-lvm2.so
          %{_libdir}/libdevmapper.so
          %{_libdir}/libdevmapper-event.so
          %{_includedir}/lvm2cmd.h
          %{_includedir}/libdevmapper.h
          %{_includedir}/libdevmapper-event.h
          %{_libdir}/pkgconfig/devmapper.pc
          %{_libdir}/pkgconfig/devmapper-event.pc
          

    - name: help
      meta.summary: Including man files for lvm2
      meta.description: This contains man files for the using of lvm2.
      requires:
          - "man"
      buildArch: noarch
      files: |
          %{_mandir}/man5/*
          %{_mandir}/man7/*
          %{_mandir}/man8/*
          

    - name: python3-lvm-deprecated
      meta.summary: Python 3 api to lvm
      meta.license: LGPLv2
      meta.description: Support python 3 to use lvm.
      requires:
          - "%{name}-libs = %{?epoch}:%{version}-%{release}"
      provides:
          - "python3-lvm = %{?epoch}:%{version}-%{release}"
          - "%{name}-python3-libs = %{?epoch}:%{version}-%{release}"
      obsoletes:
          - "%{name}-python3-libs < %{?epoch}:%{version}-%{release}"
      AsWholeName: yes

    - name: lockd %if %{use_lockd_dlm} || %{use_lockd_sanlock}
      meta.summary: LVM locking daemon %if %{use_lockd_dlm} || %{use_lockd_sanlock}
      meta.description: LVM commands use lvmlockd to coordinate access to shared storage.
      requires:
          - "lvm2 = %{?epoch}:%{version}-%{release} %if %{use_lockd_dlm} || %{use_lockd_sanlock}"
          - "sanlock-lib >= %{sanlock_version} %if %{use_lockd_dlm} || %{use_lockd_sanlock} %if %{use_lockd_sanlock}"
          - "dlm-lib >= %{dlm_version} %if %{use_lockd_dlm} || %{use_lockd_sanlock} %if %{use_lockd_dlm}"
      requiresPreUn:
          - "systemd-units >= %{systemd_version} %if %{use_lockd_dlm} || %{use_lockd_sanlock}"
      requiresPost:
          - "systemd-units >= %{systemd_version} %if %{use_lockd_dlm} || %{use_lockd_sanlock}"
      requiresPostUn:
          - "systemd-units >= %{systemd_version} %if %{use_lockd_dlm} || %{use_lockd_sanlock}"
      files: |
          %defattr(444,root,root,-)
          %%attr(555, -, -) %{_sbindir}/lvmlockd
          %%attr(555, -, -) %{_sbindir}/lvmlockctl
          %{_unitdir}/lvmlockd.service
          %{_unitdir}/lvmlocks.service
      filesJudgement:
          - %if %{use_lockd_dlm} || %{use_lockd_sanlock}

    - name: cluster %if %{use_cluster}
      meta.summary: cluster LVM daemon tools %if %{use_cluster}
      meta.license: GPLv2 %if %{use_cluster}
      meta.description: cluster distributes LVM metadata updates  around  a  cluster.
      requires:
          - "lvm2 = %{?epoch}:%{version}-%{release} %if %{use_cluster}"
          - "corosync >= %{corosync_version} %if %{use_cluster}"
          - "dlm >= %{dlm_version} %if %{use_cluster}"
          - "resource-agents >= %{resource_agents_version} %if %{use_cluster}"
      requiresPreUn:
          - "device-mapper >= %{?epoch}:%{device_mapper_version} %if %{use_cluster}"
          - "lvm2 >= 2.02 %if %{use_cluster}"
      provides:
          - "cluster-standalone %if %{use_cluster}"
      obsoletes:
          - "cluster-standalone %if %{use_cluster}"
      files: |
          %defattr(555,root,root,-)
          %{_sbindir}/clvmd
          %{_prefix}/lib/systemd/lvm2-cluster-activation
          %defattr(444,root,root,-)
          %{_unitdir}/lvm2-clvmd.service
          %{_unitdir}/lvm2-cluster-activation.service
      filesJudgement:
          - %if %{use_cluster}

    - name: cmirror %if %{use_cluster} && %{use_cmirror}
      meta.summary: cluster mirror log daemon %if %{use_cluster} && %{use_cmirror}
      meta.license: GPLv2 %if %{use_cluster} && %{use_cmirror}
      meta.description: cmirror tracks mirror log information in a cluster.
      requires:
          - "corosync >= %{corosync_version} %if %{use_cluster} && %{use_cmirror}"
          - "device-mapper = %{?epoch}:%{device_mapper_version}-%{release} %if %{use_cluster} && %{use_cmirror}"
          - "resource-agents >= %{resource_agents_version} %if %{use_cluster} && %{use_cmirror}"
      provides:
          - "cmirror-standalone %if %{use_cluster} && %{use_cmirror}"
      obsoletes:
          - "cmirror-standalone %if %{use_cluster} && %{use_cmirror}"
      files: |
          %defattr(555,root,root,-)
          %{_sbindir}/cmirrord
          %{_unitdir}/lvm2-cmirrord.service
      filesJudgement:
          - %if %{use_cluster} && %{use_cmirror}
      AsWholeName: yes

    - name: dbusd
      meta.summary: LVM2 D-Bus daemon
      meta.license: GPLv2
      meta.description: dbusd is a service which provides a D-Bus API to the logical volume manager.
      requires:
          - "lvm2 >= %{?epoch}:%{version}-%{release}"
          - "dbus"
          - "python3-dbus"
          - "python3-pyudev"
          - "python3-gobject-base"
      requiresPreUn:
          - "systemd-units >= %{systemd_version}"
      requiresPost:
          - "systemd-units >= %{systemd_version}"
      requiresPostUn:
          - "systemd-units >= %{systemd_version}"
      files: |
          %defattr(555,root,root,-)
          %{_sbindir}/lvmdbusd
          %defattr(444,root,root,-)
          %{_sysconfdir}/dbus-1/system.d/com.redhat.lvmdbus1.conf
          %{_datadir}/dbus-1/system-services/com.redhat.lvmdbus1.service
          %{_unitdir}/lvm2-lvmdbusd.service
          %{python3_sitelib}/lvmdbusd/*
          

    - name: test
      meta.summary: Testsuite for lvm2
      meta.license: LGPLv2 and GPLv2 and BSD-2-Clause
      meta.description: An extensive functional testsuite for LVM2.
      provides:
          - "%{name}-testsuite"
      obsoletes:
          - "%{name}-testsuite"
      files: |
          %license COPYING COPYING.LIB COPYING.BSD
          %{_datadir}/lvm2-testsuite/
          %{_libexecdir}/lvm2-testsuite/
          %{_bindir}/lvm2-testsuite
          
          

    - name: device-mapper-event
      meta.summary: Handle device-mapper events
      version: %{device_mapper_version}
      meta.description: |
          It contains tthe event monitoring daemon for device-mapper devices.  Library plugins
          can register and carryout actions triggered when particular events occur.
      requires:
          - "device-mapper = %{?epoch}:%{device_mapper_version}-%{release}"
          - "systemd-units"
      provides:
          - "device-mapper-event-libs = %{?epoch}:%{device_mapper_version}-%{release}"
      obsoletes:
          - "device-mapper-event-libs"
      files: |
          %license COPYING.LIB
          %defattr(555,root,root,-)
          %{_sbindir}/dmeventd
          %{_libdir}/libdevmapper-event.so.*
          %defattr(444,root,root,-)
          %{_unitdir}/dm-event.socket
          %{_unitdir}/dm-event.service
          
      AsWholeName: yes

