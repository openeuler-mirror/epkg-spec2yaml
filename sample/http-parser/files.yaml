files: |
    %defattr(-,root,root)
    %license LICENSE*
    %{_libdir}/*.so.*

subpackage.devel.files: |
    %defattr(-,root,root)
    %{_includedir}/*.h
    %{_libdir}/*.so

subpackage.help.files: |
    %defattr(-,root,root)
    %doc AUTHORS README.md