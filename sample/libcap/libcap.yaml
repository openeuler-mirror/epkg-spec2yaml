name: libcap
meta.summary: A library for getting and setting POSIX.1e draft 15 capabilities
version: 2.61
release: 3
meta.license: GPLv2
meta.homepage: https://sites.google.com/site/fullycapable
sources:
    - https://www.kernel.org/pub/linux/libs/security/linux-privs/libcap2/%{name}-%{version}.tar.gz
patchset:
    - libcap-buildflags.patch
    - Fix-syntax-error-in-DEBUG-protected-setcap.c-code.patch
    - backport-psx-free-allocated-memory-at-exit.patch
    - backport-Avoid-a-deadlock-in-forked-psx-thread-exit.patch
meta.description: |
    This is a library for getting and setting POSIX.1e (formerly POSIX 6)
    draft 15 capabilities.
buildRequires:
    - libattr-devel pam-devel perl-interpreter gcc
files: |
    %defattr(-,root,root)
    %license License
    %doc doc/capability.notes
    %{_libdir}/*.so.*
    %{_sbindir}/*
    %{_libdir}/security/pam_cap.so
    
Pre:
    - libcap.sh
Preun:
    - libcap.sh
Post:
    - libcap.sh
Postun:
    - libcap.sh
subpackage:
    - name: devel
      meta.summary: Development headers and libraries for %{name}
      meta.description: Development headers and libraries for %{name}
      files: |
          %defattr(-,root,root)
          %{_includedir}/*
          %{_libdir}/*.so
          %{_libdir}/pkgconfig/*.pc
          %{_libdir}/*.a
          

    - name: help
      meta.summary: Documents for %{name}
      meta.description: Man pages and other related documents for %{name}.
      requires:
          - man info
      buildArch: noarch
      files: |
          %defattr(-,root,root)
          %{_mandir}/man3/*.gz
          %{_mandir}/man1/*.gz
          %{_mandir}/man8/*.gz
          

