files: |
    %{_libdir}/lib%{name}.so.%{so_ver}*

subpackage.devel.files: |
    %{_includedir}/%{name}
    %{_libdir}/cmake/%{name}
    %{_libdir}/lib%{name}.so
    %{_libdir}/pkgconfig/%{name}.pc

subpackage.help.files: |
    %doc %{_pkgdocdir}