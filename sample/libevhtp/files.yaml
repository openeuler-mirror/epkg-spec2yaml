files: |
    %defattr(-,root,root)
    %license LICENSE
    /usr/lib/%{name}.so.*

subpackage.devel.files: |
    %defattr(-,root,root)
    %{_includedir}/*.h
    %{_includedir}/evhtp/*.h
    %{_includedir}/evhtp/sys/*.h
    /usr/lib/%{name}.so
    /usr/lib/pkgconfig/evhtp.pc