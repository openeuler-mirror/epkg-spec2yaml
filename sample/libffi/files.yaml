files: |
    %{_libdir}/*.so.*

subpackage.devel.files: |
    %{_libdir}/pkgconfig/*.pc
    %{_includedir}/ffi*.h
    %{_libdir}/*.so
    
subpackage.help.files: |
    %{_mandir}/man3/*.gz
    %{_infodir}/libffi.info.gz
